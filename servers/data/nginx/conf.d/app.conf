server {
    listen 80 default_server fastopen=256;
    server_name _;

    server_tokens off;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        proxy_pass https://opinion.works;
    }
}
