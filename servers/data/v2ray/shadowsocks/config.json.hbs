{
    "log": {
        "loglevel": "info"
    },
    "inbounds": [
    {{#select protocols "shadowsocks"}}
        {
            "port": {{server.port}},
            "protocol": "{{name}},
            "settings": {
                "method": "{{credentials.method}}",
                "password": "{{credentials.password}}"
            }
         }
    {{/select}}
    ],
    "outbounds": [
        {
            "protocol": "freedom",
            "settings": {
            }
        }
    ]
}
