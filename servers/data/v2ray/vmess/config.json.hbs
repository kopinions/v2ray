{
    "log": {
        "loglevel": "info"
    },
    "inbounds": [
    {{#select protocols "vmess"}}
        {
            "port": {{server.port}},
            "protocol": "{{name}},
            "settings": {
                "clients": [
                    {{#each credentials}}
                     {
                         "id": "{{id}}",
                         "alterId": {{alterId}}
                     }
                     {{#unless @last}}
                     ,
                     {{/unless}}
                    {{/each}}
                 ]
            },
            "streamSettings": {
                "network": "ws",
                "wsSettings": {
                    "path": "/v",
                    "headers": {
                        "host": "{{server.address}}"
                    }
                }
            }
         }
    {{/select}}
    ],
    "outbounds": [
        {
            "protocol": "freedom",
            "settings": {
            }
        }
    ]
}
